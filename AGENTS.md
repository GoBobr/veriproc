# Agent Guidelines

- When adding, changing, or removing functionality, always update the documentation in `docs/docs` and `docs/specs`.
- When adding new functionality, make sure it has enough test coverage, and doesn't break existing unit tests.
